export default function Card({ isiTxt, isiDesc }) {
    return (
        <>
        <div className="kartu2">
            <h1>{isiTxt}</h1>
            <p>{isiDesc}</p>
        </div>
        </>
    )
}